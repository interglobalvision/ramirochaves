$gutter = 14px

$margin-micro = ($gutter / 3)
$margin-tiny = ($gutter / 2)
$margin-small = ($gutter)
$margin-basic = ($gutter * 2)
$margin-mid = ($gutter * 3.5)
$margin-large = ($gutter * 5)

$font-size-basic = 12px
$font-size-extra = 48px
$font-size-large = 36px
$font-size-mid = 24px
$font-size-small = 18px
$font-size-tiny = 8px

grid-maker(96%, $gutter, s)

space-maker($margin-micro, $margin-tiny, $margin-small, $margin-basic, $margin-mid, $margin-large)

font-sizes($font-size-basic, $font-size-extra, $font-size-large, $font-size-mid, $font-size-small, $font-size-tiny)

body,
#main-canvas
  cursor: url("data:image/svg+xml;charset=UTF-8,%3csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 52 52' width='52' height='52'%3e%3ccircle cx='26' cy='26' r='25' fill='%23fff' stroke='%23000' stroke-width='2' /%3e%3c/svg%3e") 25 25, pointer

#header
  position: absolute
  top: 0
  left: 0
  height: 100vh
  width: 100vw
  display: flex
  flex-direction: column
  justify-content: center
  text-align: center
  pointer-events: none

#main-canvas
  position: absolute
  top: 0
  left: 0

#footer
  position: fixed
  bottom: 0
  left: 0
  width: 100vw
